/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import FoodAppManager from './src/FoodAppManager';

AppRegistry.registerComponent(appName, () => FoodAppManager);
